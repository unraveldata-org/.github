name: Jira ID Enforcer

on:
  workflow_call:
  pull_request:
    types: [opened, edited, synchronize]

permissions:
  contents: read
  pull-requests: read

jobs:
  jira-validation:
    name: "Jira ID Validation"
    runs-on: ubuntu-latest
    concurrency:
      group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.run_id }}
      cancel-in-progress: true
    steps:
      - name: Check Title
        shell: bash
        run: |
          TITLE="${{ github.event.pull_request.title }}"
          echo "Validating Title: $TITLE"
         
          if [[ "$TITLE" =~ [A-Z0-9]+-[0-9]+ ]]; then
            echo "✅ Pass: Valid Jira ID found."
            exit 0
          else
            echo "❌ Fail: PR Title must have a Jira ID (e.g. PROJ-123 or U49-341)."
            exit 1
          fi
